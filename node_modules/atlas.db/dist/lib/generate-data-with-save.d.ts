import { Structure } from '../entities/Structure';
import { TableSchema } from '../types/TableSchema';
export default function generateDataWithSave<StructureData, TableData extends TableSchema<StructureData>>(path: string, tableData: TableData, structure: Structure<StructureData>): { [key in keyof StructureData]: StructureData[key]; } & {
    save: () => any;
    id: any;
};
